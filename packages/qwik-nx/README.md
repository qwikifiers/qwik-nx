# qwik-nx

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build qwik-nx` to build the library.

## Running unit tests

Run `nx test qwik-nx` to execute the unit tests via [Jest](https://jestjs.io).
